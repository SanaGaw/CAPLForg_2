"""Streamlit env var browser view."""
import streamlit as st

st.set_page_config(page_title="Env Var Browser", page_icon="🔌")

st.title("Env Var Browser")
st.info("Env var browser — connect a knowledge base to browse environment variables.")
